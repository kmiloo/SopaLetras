package com.miempresa.sopaletras.di

/**
 * Paquete reservado para la configuración de Inyección de Dependencias.
 *
 * En la primera entrega las dependencias se instancian manualmente en `MainActivity`.
 * En entregas futuras se reemplazará por módulos de Hilt o Koin, por ejemplo:
 *
 *   @Module
 *   @InstallIn(SingletonComponent::class)
 *   object ModuloAplicacion { ... }
 */
internal object MarcadorPaqueteDi
